namespace FSM.Utilities {
    public interface IFactory : IResettable {
        // nothing here now, just want a unified interface for
        // whatever may end up here or a way to group interfaces
        // which make up certain things (like factories!)
    }
}